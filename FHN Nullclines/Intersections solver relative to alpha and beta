import sympy as sym


def intersections(alpha, beta):
    r,v = sym.symbols('r,v')
    f = sym.Eq(v * (alpha - v) * (v - 1), r)
    g = sym.Eq(1/beta*v,r)
    print(sym.solve([f,g],(r,v)))

#Use intersections(alpha value here!, beta value here!) for generating intersectionpoints in the phaseplane
